services:
  zooluck:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: zooluck-app
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      - NODE_ENV=production
      - PORT=3001
    volumes:
      # 持久化数据库文件
      - ./data:/app/server/data
      # 持久化日志
      - ./logs:/var/log/nginx
    networks:
      - zooluck-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  zooluck-network:
    driver: bridge
